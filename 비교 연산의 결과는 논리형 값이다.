x = 5              # 변수 x에 5를 저장한다
z = x < 1          # x가 1보다 작은지 비교한 결과(True/False)를 z에 저장한다
print(z)           # z의 값을 출력한다 (x=5이므로 False가 출력됨) 거짓
print(x > 0)       # x가 0보다 큰지 비교한 결과를 출력한다 (x=5이므로 True가 출력됨) 참
